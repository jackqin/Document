Global setup:

 Set up git
  git config --global user.name "jackqin"
  git config --global user.email followfollowme@163.com
        

Next steps:

  mkdir aaaaaa
  cd aaaaaa
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:jackqin/aaaaaa.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:jackqin/aaaaaa.git
  git push -u origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue


